# Batas memory

Kontrak referensi memory dan retention policy masa depan dapat ditempatkan di sini. Repository ini tidak akan mengimplementasikan memory storage, retrieval infrastructure, embedding, atau vector database.
